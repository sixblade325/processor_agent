# processor_agent 协作规则

## 1. 仓库职责

1. 本仓库维护通用处理器开发工作流、Schema、模板、Skill、适配器和工作台代码。
2. 具体处理器的架构事实、模块设计、源码、测试和实验结论进入对应用户项目。
3. 龙芯杯项目材料只作为来源和案例，框架中不复制其源码与设计正文。
4. 用户最新明确指令优先。

## 2. 文档层级

1. `PRODUCT_PLAN/V2/PRODUCT_PLAN.md` 是产品总纲，维护产品目标、边界、整体架构、范围、里程碑和验收标准。
2. `PRODUCT_PLAN/V2/STAGE1.md`、`STAGE2.md` 和 `STAGE3.md` 分别维护对应阶段的权威计划。
3. 阶段计划服从产品总纲，阶段内部流程以对应阶段计划为准。
4. `idea/` 保存历史讨论、原始构想和图示，不承担正式产品事实。
5. 同一事实只在一个权威文档中完整定义，其他文档使用摘要和链接。
6. 用户操作入口为仓库根目录 `USER_GUIDE.md`，CLI 行为变化时同步更新。

## 3. 项目边界

1. 框架读取用户项目时，先读取该项目的 `AGENTS.md`。
2. 项目内 Architecture 和 Design 决定实现约束，框架默认值不能覆盖项目事实。
3. 框架只通过明确接口读写用户项目，不依赖本机绝对路径。
4. 通用逻辑不得硬编码 `dual_issue_demo` 或龙芯杯项目的模块名、信号名和流水级。
5. 一个正式 Skill 只保留一份。
6. 用户项目的人类可读文档默认使用中文，模块名、信号名、字段名、文件名、命令和代码保持英文。
7. 新项目缺少 `AGENTS.md` 时生成严格协作约束，已有 `AGENTS.md` 默认保留并由用户确认是否升级。

## 4. Skill 规则

1. 每个 Skill 使用独立目录，并以 `SKILL.md` 为入口。
2. Skill 描述方法、输入、输出、门禁和检查项。
3. 项目专属事实、源码摘录、临时搜索结果和运行日志不得进入 Skill。
4. 从遗产提炼 Skill 时，先区分通用规则与 WaterHand、LoongArch、Zircon 专属规则。
5. 修改 Skill 后执行结构检查，并记录受影响的工作流。

## 5. 运行数据

1. 缓存、生成文件、临时工作树、波形和原始日志进入工作区级 `.runtime/`。
2. 可复现结论晋升到用户项目的正式文档后，删除重复草案。
3. Git 管理版本，不创建 `v1`、`v2` 和 `final` 目录。
4. 未经用户确认，不清理遗产项目中的现有缓存或生成物。

## 6. 变更要求

1. 修改范围保持在当前功能涉及的模块内。
2. 新增目录、Schema 或抽象前说明其长期职责。
3. 代码变更需要相应测试，工作流变更需要最小端到端样例。
4. 交付时报告修改文件、验证命令、验证结果和未解决问题。
