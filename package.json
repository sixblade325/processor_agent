{
  "name": "processor-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "processor-agent": "./dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/tests/*.test.js\"",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=22"
  }
}
